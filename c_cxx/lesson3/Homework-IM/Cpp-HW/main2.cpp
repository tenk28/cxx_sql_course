/*#include <iostream>

int sum(int n){
    int sum = 0;
    for (int i = 1; i <= n; ++i) {
        sum = sum + (i + 1);
    }
    return sum;
}

int main() {
    int n;

    do {
        std::cout << "Enter value for n(n >= 0): ";
        std::cin >> n;
    } while (!(n >= 0));
    int result = sum(n);
    std::cout << " sum(n,x) = " << sum(n);

    return 0;
}
*/